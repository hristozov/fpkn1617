(load "../../lib/scm/unit.scm")

; Факториел на число.
; Забележете, че се придържаме към рекурсивната дефиниция на функцията за
; факториел, позната ни от математиката.
(define (fact n)
  (if (<= n 1)
      1
      (* n (fact (- n 1)))))

(assert= 1 (fact 1))
(assert= 24 (fact 4))
